{
  "name": "SocialRiskScanner",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run dev -- --host 0.0.0.0",
    "build": "npm --prefix client run build",
    "client:dev": "npm --prefix client run dev -- --host 0.0.0.0"
  }
}
